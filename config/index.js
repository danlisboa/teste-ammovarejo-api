const PORT = 4000;

module.exports = {
  wellcome_msg : `API test ammovarejo is running ${PORT}`,
  port : PORT,
  bd : {
    url: '',
    msg_connected: 'mongo connected',
    msg_not_connected: 'mongo not connected'
  }
}